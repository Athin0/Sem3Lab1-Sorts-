//
// Created by arina on 02.10.2021.
//

#ifndef SORTS_MENU_H
#define SORTS_MENU_H

#include <iostream>
#include "DynamicArraySequence.h"
#include "LinkedListSequence.h"
#include "Compare.h"
#include "baseFunc.h"
#include "Tests.h"

void mainMenuSequence();

/*
int getType();

//1
template<class T>
void readSequence(ArraySequence<ArraySequence<T> *> *arrDA,
                  ArraySequence<ArraySequence<T> *> *arrLL);

template<class T>
void readTypeSequence(ArraySequence<Sequence<T>*> *arr, int count);

template<class T>
void readTypeSequence(Sequence<ArraySequence<T>*> *arr, int count);

template<class T>
void readTypeSequence(Sequence<LinkedListSequence<T>*> *arr, int count);




template<class T>
void generateRandomSequence(ArraySequence<LinkedListSequence<T>*> *arr, int count, T (*func)());


template<class T>
void generateRandomSequence(ArraySequence<ArraySequence<int>*> *arr, int count, int (*func)());

//2
void operationWithSequence(ArraySequence<ArraySequence<int> *> *arrDA,
ArraySequence<LinkedListSequence<int> *> *arrLL);

template<class T>
void operationTypeWithSequence(ArraySequence<Sequence<T>*> *arr);

//3
void printSequence(ArraySequence<ArraySequence<int> *> *arrDA,
ArraySequence<LinkedListSequence<int> *> *arrLL);

template<class T>
void printTypeSequence(ArraySequence<Sequence<T>*> *arr);


//4
void deleteSequence(ArraySequence<ArraySequence<int> *> *arrDA,
ArraySequence<LinkedListSequence<int> *> *arrLL);

template<class T>
void deleteTypeSequence(ArraySequence<Sequence<T>*> *arr);


void
printSequence(ArraySequence<ArraySequence<int> *> *pSequence, ArraySequence<LinkedListSequence<int> *> *pSequence1);

void
deleteSequence(ArraySequence<ArraySequence<int> *> *pSequence, ArraySequence<LinkedListSequence<int> *> *pSequence1);

void readTypeSequence(ArraySequence<ArraySequence<int> *> *pSequence, int count);

void generateRandomSequence(ArraySequence<ArraySequence<int> *> *pSequence, int count, int (*anInt)());


void printTypeSequence(ArraySequence<Sequence<int> *> *pSequence);

void deleteTypeSequence(ArraySequence<Sequence<int> *> *pSequence);

*/
#endif //SORTS_MENU_H
